name: Deploy MkDocs para GitHub Pages

on:
  push:
    branches:
      - main
      - master
    paths:
      - "mkdocs.yml"
      - "docs/**"
  workflow_dispatch: # permite execução manual pela aba Actions

permissions:
  contents: write # necessário para publicar no branch gh-pages

jobs:
  deploy:
    name: Build e Deploy da Documentação
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # histórico completo necessário para o plugin git-dates

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
          cache: pip
          cache-dependency-path: "**/requirements*.txt"

      - name: Instalar MkDocs e dependências
        run: |
          pip install -r requirements.txt

      - name: Build e deploy para GitHub Pages
        run: |
          mkdocs gh-deploy --force --clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
